import { isEligible, matchName, scoreOf } from "../utils/formatters";
import styles from "./MatchCard.module.css";
export default function MatchCard({ match, onView, featured }) { const eligible = isEligible(match); const reasons = Array.isArray(match?.passed) ? match.passed.slice(0, 3) : []; return <article className={`${styles.card} ${featured ? styles.featured : ""}`}><div className={styles.top}><div><span className={styles.category}>{match?.category || "Opportunity"}</span><h3>{matchName(match)}</h3><p>{match?.provider || match?.organization || "Provider not specified"}</p></div><div className={styles.score}><strong>{scoreOf(match)}%</strong><span>match</span></div></div>{eligible !== null && <p className={eligible ? styles.eligible : styles.ineligible}>{eligible ? "✓ Eligible" : "✕ May not be eligible"}</p>}{reasons.length > 0 && <ul>{reasons.map((reason, index) => <li key={index}>✓ {reason}</li>)}</ul>}<button onClick={onView}>View details <span>→</span></button></article>; }
