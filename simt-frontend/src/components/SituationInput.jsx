import { useState } from "react";
import styles from "./SituationInput.module.css";
export default function SituationInput({ onSubmit }) { const [value, setValue] = useState(""); const [error, setError] = useState(""); const submit = (event) => { event.preventDefault(); if (!value.trim()) return setError("Please tell us a little about your situation first."); onSubmit(value.trim()); }; return <section className={styles.wrap}><span className={styles.eyebrow}>START HERE</span><h1>Tell us about your situation.</h1><p>Use your own words. Mention things like your city, current work or studies, goals, and the support you are looking for.</p><form onSubmit={submit}><label htmlFor="situation">Your situation</label><textarea id="situation" value={value} onChange={(event) => { setValue(event.target.value); setError(""); }} placeholder="I am 28 years old, live in Gujranwala, currently unemployed, and want to start an online clothing business. I need around Rs. 12 lakh." rows="8" aria-describedby="input-help input-error" /><div className={styles.meta}><span id="input-help">Share only what is relevant. Do not include CNIC, bank details, or passwords.</span><span>{value.length}/1200</span></div>{error && <p id="input-error" className={styles.error} role="alert">{error}</p>}<button type="submit">Find My Opportunities <span>→</span></button></form></section>; }
