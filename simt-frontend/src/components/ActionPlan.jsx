import styles from "./ActionPlan.module.css";
export default function ActionPlan({ match }) { const raw = match?.action_plan || match?.next_steps || match?.steps; const steps = Array.isArray(raw) ? raw : typeof raw === "string" ? [raw] : []; const source = match?.official_source || match?.official_url || match?.source_url; return <section className={styles.section}><div><span>ACTION PLAN</span><h2>What to do next</h2>{steps.length ? <ol>{steps.map((step, index) => <li key={index}><b>{String(index + 1).padStart(2, "0")}</b><span>{typeof step === "string" ? step : step?.text || step?.description || "Review this step"}</span></li>)}</ol> : <p className={styles.empty}>Review the program information and confirm its current requirements with the official source.</p>}</div>{source && <a href={source} target="_blank" rel="noopener noreferrer">Visit Official Source <span>↗</span></a>}</section>; }
