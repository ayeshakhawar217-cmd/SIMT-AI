import styles from "./WhyMatch.module.css";
const List = ({ title, values, tone }) => values.length ? <section className={`${styles.list} ${styles[tone]}`}><h2>{title}</h2><ul>{values.map((item, index) => <li key={index}>{tone === "good" ? "✓" : tone === "bad" ? "×" : "•"}<span>{typeof item === "string" ? item : item?.reason || item?.message || "Detail available"}</span></li>)}</ul></section> : null;
export default function WhyMatch({ match }) { const passed = Array.isArray(match?.passed) ? match.passed : []; const failed = Array.isArray(match?.failed) ? match.failed : []; const notApplicable = Array.isArray(match?.not_applicable) ? match.not_applicable : []; const explanation = match?.why_match || match?.explanation || match?.description; return <section className={styles.section}><div className={styles.heading}><span>WHY THIS RESULT</span><h2>Clear reasoning, not a black box.</h2>{explanation && <p>{explanation}</p>}</div><div className={styles.grid}><List title="Why it may fit" values={passed} tone="good" /><List title="Things to check" values={failed} tone="bad" /><List title="Requirements to Check" values={notApplicable} tone="neutral" /></div></section>; }
